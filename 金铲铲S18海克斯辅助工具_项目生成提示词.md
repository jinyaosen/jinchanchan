# 金铲铲 S18「拼羁绊」海克斯辅助工具 — 项目生成提示词（修正版）

## 角色与目标
你是一位精通 React/TypeScript 算法工程与游戏策略系统的前端架构师。请根据我提供的 `champions.json` 和 `traits.json` 数据，生成一个**完整的、可直接运行的单页 Web 应用**。

该应用核心解决两个独立优化问题：
1. **羁绊数量最大化阵容**：在限定人口和转职下，凑出激活**通用羁绊**数量最多的阵容（专属羁绊不计入目标函数，但拉克丝的"大元素使"机制需要特殊处理）。
2. **阵容质量最强阵容**：在限定人口和转职下，找到综合战力评分最高的阵容（基于费用、羁绊层级、功能性加权）。

---

## 1. 输入数据格式（严格遵循，不得修改字段名）

### 1.1 champions.json — 英雄表
```json
[
  {
    "id": "lux",
    "name": "拉克丝",
    "cost": 5,
    "traits": ["灵魂莲华", "地狱火", "永恒之森", "古神", "仙灵", "远古", "日食", "月食", "魔女", "大元素使"],
    "slots": 1
  },
  {
    "id": "ornn",
    "name": "奥恩",
    "cost": 4,
    "traits": ["永恒之森", "护卫"],
    "slots": 1
  }
]
```
- `slots`: 占用人口（通常为1，远古巨龙等可能为2）
- **注意**：S18 没有"天选英雄"机制，移除 `isChosen` 字段

### 1.2 traits.json — 羁绊表
```json
[
  {
    "id": "spirit",
    "name": "灵魂莲华",
    "thresholds": [3, 5, 7, 9, 11],
    "hasEmblem": true,
    "emblemName": "灵魂莲华纹章",
    "type": "general"
  },
  {
    "id": "elementalist",
    "name": "大元素使",
    "thresholds": [1],
    "hasEmblem": false,
    "type": "exclusive",
    "effect": {
      "description": "拉克丝登场时，从以下羁绊中随机选择一个获得双倍计数",
      "targetTraits": ["灵魂莲华", "日食", "月食", "地狱火", "黑荆棘", "永恒之森", "魔女", "花仙子", "野兽之灵"]
    }
  }
]
```
- `thresholds`: 激活阈值，如 `[2,4,6]` 表示 2/4/6 人口时分别激活
- `hasEmblem`: 该羁绊是否可通过金铲铲合成转职纹章
- `type`: 羁绊类型，枚举值为 `"general"`（通用，如职业/特质）或 `"exclusive"`（专属，如"大元素使"、"宿敌"、"顶级掠食者"等）。**只有 `type="general"` 的羁绊才计入"羁绊数量"目标函数。**
- `effect`: 专属羁绊的特效描述（仅 `type="exclusive"` 时存在），用于 UI 展示和算法特殊处理

---

## 2. 核心功能需求

### 2.1 全局输入面板
- **人口上限 Slider**：范围 1~15，默认 10
- **转职纹章数量 Slider**：范围 0~10，默认 2
- **拉克丝"大元素使"配置**（条件渲染，仅当阵容中包含拉克丝时显示）：
  - 下拉选择拉克丝触发的双倍羁绊（从 `effect.targetTraits` 中选择一个）
  - 默认不选（视为未触发双倍，或用户手动指定）
  - 若未选择，算法按"不触发双倍"计算；若选择，该羁绊计数额外+1（即拉克丝对该羁绊贡献2点而非1点）
- **锁定英雄面板**：多选框，强制某些英雄必须上场（受人口约束，若锁定英雄总 slots > 人口则提示错误）
- **排除英雄面板**：多选框，某些英雄不参与计算
- **可用转职羁绊面板**：从所有 `hasEmblem=true` 且 `type="general"` 的羁绊中多选，表示玩家实际拥有的转职类型（未选中的羁绊即使有纹章也不能分配）
- **转职分配策略**：
  - 每个转职纹章可以分配给阵容中任意一个英雄
  - 一个英雄可以携带多个不同羁绊的转职
  - 转职使对应羁绊计数 +1

### 2.2 求解模式 A：羁绊数量最大化（Max Traits）
**目标函数**：
```
maximize: 激活的 general 羁绊数量（去重计数，不重复计算同一羁绊的多级阈值）
```
即：只要某 general 羁绊的当前计数 ≥ thresholds 中任意一个值，就算激活 1 个羁绊。目标是在人口和转职约束下，让激活的 general 羁绊总数最大。

**拉克丝特殊规则**：
- 若用户在 UI 中指定了拉克丝的"大元素使"触发某个羁绊 X：
  - 拉克丝对羁绊 X 的计数贡献为 2（而非 1）
  - 羁绊 X 必须是 `effect.targetTraits` 中的成员
  - "大元素使"本身作为 exclusive 羁绊不计入目标函数，但其触发的双倍效果影响目标羁绊的计数
- 若未指定（或阵容中无拉克丝），拉克丝对所有羁绊贡献均为 1

**约束条件**：
- Σ(选中英雄的 slots) + 转职占用（转职不占人口，0 slot）≤ 人口上限
- 转职分配数 ≤ 转职纹章总数
- 锁定英雄必须包含在结果中
- 排除英雄不能出现在结果中

**输出要求**：
- 显示推荐阵容（英雄头像/名称/费用/占用人口）
- 显示所有激活的 general 羁绊列表（高亮显示靠转职激活的羁绊，以及靠拉克丝双倍触发的羁绊）
- 显示阵容占用人口 / 总人口的进度条
- 显示"差一点激活"的羁绊（当前计数距离下一个 threshold 差几个）
- 若存在多个最优解（相同羁绊数），优先展示费用总和更高的方案（作为 tie-breaker）

### 2.3 求解模式 B：阵容质量最强（Max Quality）
**目标函数**：
```
QualityScore = Σ(英雄费用分) + Σ(羁绊层级分) + 功能性加成
```

各分项定义：
- **英雄费用分**：`cost * 10`（5费=50分，1费=10分）
- **羁绊层级分**：每个激活的 general 羁绊，根据其达到的最高 threshold 索引给分：
  - 达到 thresholds[0]：+15 分
  - 达到 thresholds[1]：+35 分（累计）
  - 达到 thresholds[2]：+60 分（累计）
  - 达到 thresholds[3]：+90 分（累计）
  - 达到 thresholds[4]：+120 分（累计）
  - 即：层级越高，边际收益越大
- **功能性加成**：
  - 若阵容包含至少1个5费英雄：+20 分
  - 若阵容包含至少3个4费及以上英雄：+15 分
  - 若激活羁绊数 ≥ 8：+25 分
  - 若人口利用率 ≥ 90%：+10 分

**拉克丝加成**：若阵容中包含拉克丝且指定了双倍羁绊，该羁绊因双倍而额外激活更高层级时，层级分按实际达到的层级计算（即双倍帮助冲高阶羁绊时获得更高分）。

**约束条件**：与模式 A 相同

**输出要求**：
- 除模式 A 的基础信息外，额外显示：
  - 阵容总质量分及分项明细（费用分 / 羁绊分 / 功能分）
  - 阵容平均费用
  - 阵容中各费用段英雄分布图（1费/2费/3费/4费/5费 各几个）

---

## 3. 算法架构要求（关键）

### 3.1 整体架构
- 使用 **Web Worker** 执行求解计算，避免阻塞主线程 UI
- 求解过程显示进度条和预估时间，支持用户**中途取消**
- 采用**渐进式渲染**：先快速返回贪心近似解显示在界面上，后台继续搜索更优解，找到更好的立即更新界面

### 3.2 数据结构优化
- 羁绊使用 **BitInt / 位掩码** 编码：为每个 general 羁绊分配一个位位置，英雄羁绊集合压缩为一个 BigInt
- 人口使用 Uint8Array 追踪
- 转职分配使用贪心预分配 + 回溯精调策略

### 3.3 求解器实现（两套独立 Solver）

#### Solver A: MaxTraitsSolver
```typescript
interface SolverAState {
  selectedHeroes: Champion[];
  traitCounts: Map<TraitName, number>;
  emblemAllocations: Map<TraitName, number>; // 该羁绊分配了几个转职
  usedPopulation: number;
  activeTraitCount: number; // 只计 general
  luxDoubleTrait: string | null; // 拉克丝双倍触发的羁绊名
}
```

算法流程：
1. **预处理**：
   - 过滤排除英雄
   - 检测阵容中是否包含拉克丝（`id === "lux"` 或 `traits` 包含 "大元素使"）
   - 若包含拉克丝且用户指定了 `luxDoubleTrait`，在计算 traitCounts 时对该羁绊额外 +1
   - 计算每个英雄的"羁绊密度" = 该英雄拥有的 general 羁绊数 / slots
   - 按羁绊密度降序排列候选英雄
   - 若存在锁定英雄，先加入锁定英雄，更新初始状态

2. **Phase 1 - 贪心快速解**（< 100ms）：
   - 依次选择"新增 general 羁绊效率最高"的英雄
   - 效率 = (新增激活羁绊数) / slots
   - 拉克丝的处理：若已指定双倍羁绊，计算效率时考虑该羁绊的额外+1贡献
   - 快速得到一个可行解作为 best 下界

3. **Phase 2 - 回溯分支定界**（时间上限可配置，默认 3 秒）：
   - DFS 搜索英雄组合，按预处理排序遍历
   - **剪枝策略**（必须全部实现）：
     a. **人口剪枝**：usedPopulation + nextHero.slots > 人口上限
     b. **上界剪枝**：当前已激活羁绊数 + 剩余英雄中理论上最多还能新增的 general 羁绊数 ≤ 当前最优解
     c. **对称剪枝**：同费用同羁绊组合的英雄，按索引顺序选择，避免排列重复
     d. **转职上界剪枝**：即使把剩余所有转职都用于激活新羁绊，也无法超越当前最优
   - **转职分配**：在叶子节点（无法继续加英雄时），用贪心将转职分配给"差1个就激活 general 羁绊"的情况
   - **拉克丝处理**：若阵容中有拉克丝，在叶子节点额外评估：若将双倍羁绊切换为其他 targetTraits 中的羁绊，是否能增加激活羁绊数。若用户未指定双倍羁绊，算法应自动选择使羁绊数最大化的那个（作为优化的一部分）

4. **Phase 3 - 局部搜索优化**（可选）：
   - 对回溯得到的最优解，尝试单英雄替换（swap one hero with one outside）
   - 若替换后羁绊数增加或不变但费用更高，接受替换
   - 进行 2~3 轮直到收敛

5. **Tie-breaker**：当多个解羁绊数相同时，选择 `Σ(cost)` 更高的方案

#### Solver B: MaxQualitySolver
```typescript
interface SolverBState {
  selectedHeroes: Champion[];
  traitCounts: Map<TraitName, number>;
  emblemAllocations: Map<TraitName, number>;
  usedPopulation: number;
  qualityScore: number;
  luxDoubleTrait: string | null;
}
```

算法流程：
1. **与 Solver A 相同的预处理**（含拉克丝检测和双倍羁绊处理）
2. **Phase 1 - 贪心快速解**：
   - 选择标准改为"边际质量分 / slots"最高
   - 边际质量分 = 加入该英雄后，(费用分增量 + 新增羁绊层级分增量 + 可能触发的功能分增量)
   - 拉克丝的双倍羁绊影响：若指定双倍羁绊 X，加入拉克丝时对羁绊 X 的层级分增量按双倍后的计数计算
3. **Phase 2 - 回溯分支定界**：
   - 与 Solver A 类似，但上界剪枝改为：
     - 当前质量分 + 剩余人口 * 单位人口最大可能质量分 ≤ 当前最优质量分
     - 单位人口最大可能质量分 = 最高费用英雄的单人口分 + 理论最大羁绊分 / 人口
   - 叶子节点进行转职分配，计算完整质量分
   - 拉克丝自动选择：若用户未指定双倍羁绊，在叶子节点遍历所有 `targetTraits`，选择使质量分最高的那个
4. **Phase 3 - 局部搜索**：同 Solver A，但优化目标为质量分

### 3.4 转职分配通用子函数
```typescript
function allocateEmblems(
  traitCounts: Map<string, number>,
  availableEmblems: number,
  allowedTraits: string[], // hasEmblem=true 且用户选中的
  traitThresholds: Map<string, number[]>
): Map<string, number> {
  // 贪心策略：优先分配给"差1个就激活"的 general 羁绊
  // 若多个，优先 thresholds 更长的（后期收益大）
  // 返回分配方案 Map<羁绊名, 分配数量>
}
```

### 3.5 拉克丝羁绊计数函数
```typescript
function calculateTraitCounts(
  heroes: Champion[],
  luxDoubleTrait: string | null,
  traitMap: Map<string, Trait>
): Map<string, number> {
  // 基础计数：遍历所有英雄，对每个 general 羁绊 +1
  // 拉克丝特殊处理：
  //   - 若 luxDoubleTrait 不为 null 且该英雄是拉克丝
  //   - 且 luxDoubleTrait 是 general 类型
  //   - 则该羁绊计数额外 +1（总计 +2）
  // 注意：大元素使本身作为 exclusive 不计入，但其双倍效果影响 general 羁绊
}
```

---

## 4. UI/UX 设计规范

### 4.1 整体风格
- **深色主题**：背景 `#0B0E14`，卡片 `#151A24`，边框 `#2A3142`
- **主色调**：金色 `#C9A96E`（用于按钮、高亮、激活羁绊）
- **文字层级**：主文字 `#E8EAED`，次要 `#8B92A8`，禁用 `#4A5568`
- **费用颜色**：1费灰 `#7A7A7A`，2费绿 `#2ECC71`，3费蓝 `#3498DB`，4费紫 `#9B59B6`，5费金 `#F1C40F`
- **拉克丝专属色**：棱彩/彩虹色渐变边框 `#FF6B6B → #4ECDC4 → #45B7D1 → #96CEB4 → #FFEAA7`，用于标识"大元素使"双倍效果

### 4.2 页面布局（三栏响应式）
```
+--------------------------------------------------+
|  顶部导航：模式切换 [羁绊最多] [质量最强]          |
+---------------+------------------+-----------------+
|               |                  |                 |
|  左侧输入面板  |   中间结果展示区  |  右侧详情面板   |
|  (人口/转职/   |  (阵容卡片/      |  (羁绊明细/     |
|   拉克丝/锁定/ |   羁绊列表/      |   质量分拆解/   |
|   排除/转职)   |   人口进度条)    |   可替换建议)   |
|               |                  |                 |
+---------------+------------------+-----------------+
```
- 移动端：三栏折叠为单栏垂直布局

### 4.3 左侧输入面板组件
- **人口 Slider**：带数字显示，1~15
- **转职 Slider**：带数字显示，0~10
- **拉克丝"大元素使"配置**（条件渲染）：
  - 仅当 `champions.json` 中存在 `id="lux"` 或存在 `traits` 包含 "大元素使" 的英雄时显示
  - 下拉选择触发的双倍羁绊（选项从 `traits.json` 中 "大元素使" 的 `effect.targetTraits` 读取）
  - 选项显示为：羁绊名 + 该羁绊当前阈值预览（如"灵魂莲华（3/5/7/9/11）"）
  - 支持"自动最优"选项：让算法自动选择使目标最大化的双倍羁绊（默认选中此项）
  - 选择后，拉克丝头像旁显示小标签标识当前双倍羁绊
- **锁定/排除英雄**：可搜索的多选下拉框，选中后显示为 tag，可点击删除
- **可用转职羁绊**：网格状 tag 选择，选中的显示金色边框
- **计算按钮**：大号金色按钮，点击后显示加载动画"正在计算最优组合..."
- **取消按钮**：计算过程中显示，红色

### 4.4 中间结果展示区
- **阵容卡片**：横向排列英雄头像（圆形，带费用颜色边框），下方显示名称和费用
  - 拉克丝头像若触发了双倍羁绊，显示**彩虹色渐变边框**和一个小角标显示双倍羁绊名
- **人口进度条**：可视化显示 used / total，超过时变红色
- **激活羁绊网格**：每个羁绊显示为卡片，包含：
  - 羁绊名称
  - 当前计数 / 激活的 threshold（如 5/7）
  - 进度条显示距离下一级还有多远
  - 若该羁绊靠转职激活，显示小徽章"转"
  - 若该羁绊靠拉克丝双倍触发，显示小徽章"双"（彩虹色）
- **差一点激活区**：灰色卡片，显示当前计数和距离下一个 threshold 差多少

### 4.5 右侧详情面板（模式 B 特有）
- **质量分仪表盘**：环形进度条显示总分
- **分项柱状图**：费用分 / 羁绊分 / 功能分 的横向柱状对比
- **费用分布**：迷你柱状图显示 1~5费英雄数量
- **拉克丝贡献面板**：显示拉克丝双倍羁绊带来的额外分数/羁绊数贡献
- **优化建议**："将 [英雄A] 替换为 [英雄B]，质量分可从 X 提升到 Y"

### 4.6 交互细节
- 计算过程中，左侧输入面板置灰不可修改
- 结果支持"固定某个英雄，重新计算"（一键将该英雄加入锁定并重新跑）
- 支持"分享阵容"：生成 URL 参数，包含所有输入配置，刷新页面可恢复
- 支持"导出图片"：将当前阵容卡片导出为 PNG（使用 html2canvas 或类似方案）

---

## 5. 技术栈要求

- **框架**：React 18 + TypeScript（严格模式）
- **构建工具**：Vite
- **样式**：Tailwind CSS（不使用 UI 组件库如 AntD/Material，保持轻量）
- **状态管理**：Zustand（轻量，适合本应用）
- **图标**：Lucide React
- **Worker**：原生 Web Worker（不依赖 comlink，保持零额外依赖）
- **数据持久化**：localStorage 保存用户最后一次的输入配置
- **数据导入**：提供 JSON 文件上传区域，用户上传 `champions.json` 和 `traits.json` 后应用立即生效

---

## 6. 代码结构要求

```
src/
├── main.tsx
├── App.tsx
├── store/
│   └── gameStore.ts          # Zustand：人口、转职、拉克丝配置、锁定、排除、转职选择
├── data/
│   ├── types.ts              # Champion, Trait, SolverResult 等接口
│   ├── defaultData.ts        # 默认空数据/示例数据占位
│   └── loader.ts             # JSON 文件解析与校验
├── solvers/
│   ├── common.ts             # 转职分配、traitCounts 计算（含拉克丝）、激活判断等共享函数
│   ├── greedy.ts             # 两套贪心快速解
│   ├── backtrackA.ts         # Solver A 回溯核心（Web Worker 入口）
│   ├── backtrackB.ts         # Solver B 回溯核心（Web Worker 入口）
│   └── worker.ts             # Web Worker 封装与通信协议
├── components/
│   ├── Layout.tsx            # 三栏布局
│   ├── InputPanel.tsx        # 左侧输入面板
│   ├── ResultPanel.tsx       # 中间结果展示
│   ├── DetailPanel.tsx       # 右侧详情
│   ├── HeroCard.tsx          # 英雄头像卡片（含拉克丝彩虹边框）
│   ├── TraitBadge.tsx        # 羁绊徽章（激活/未激活/转职/双倍）
│   ├── QualityBreakdown.tsx  # 质量分拆解（模式B）
│   ├── LuxConfig.tsx         # 拉克丝大元素使配置组件
│   ├── JsonUploader.tsx      # JSON 数据上传组件
│   └── ProgressBar.tsx       # 进度条组件
└── utils/
    ├── bitmask.ts            # 羁绊位掩码编码/解码
    ├── scoring.ts            # 评分函数、threshold 查询
    └── luxEffect.ts          # 拉克丝大元素使特效处理逻辑
```

---

## 7. 关键接口定义（TypeScript，必须严格实现）

```typescript
// data/types.ts

export interface Champion {
  id: string;
  name: string;
  cost: number;
  traits: string[];
  slots: number;
}

export interface TraitEffect {
  description: string;
  targetTraits: string[]; // 大元素使的目标羁绊列表
}

export interface Trait {
  id: string;
  name: string;
  thresholds: number[];
  hasEmblem: boolean;
  emblemName?: string;
  type: 'general' | 'exclusive';
  effect?: TraitEffect; // 仅 exclusive 类型可能有
}

export interface GameConfig {
  population: number;        // 1~15
  emblemCount: number;       // 0~10
  luxDoubleTrait: string | null; // 拉克丝双倍触发的羁绊名，null 表示由算法自动选择
  lockedHeroIds: string[];
  excludedHeroIds: string[];
  allowedEmblemTraits: string[]; // 用户拥有的转职类型
}

export interface SolverResult {
  heroes: Champion[];
  traitCounts: Record<string, number>;      // 每个羁绊的原始计数（已含拉克丝双倍）
  emblemAllocations: Record<string, number>; // 转职分配
  usedPopulation: number;
  activeTraits: string[];    // 激活的 general 羁绊名列表
  inactiveTraits: { name: string; count: number; nextThreshold: number; diff: number }[];
  luxDoubleTrait: string | null; // 实际使用的拉克丝双倍羁绊（若为算法自动选择，返回选中的那个）
  // 模式B特有：
  qualityScore?: number;
  scoreBreakdown?: {
    costScore: number;
    traitScore: number;
    bonusScore: number;
  };
}

export interface WorkerMessage {
  type: 'progress' | 'result' | 'error';
  payload: any;
}
```

---

## 8. 边界情况处理

- **数据缺失**：若用户未上传 JSON，显示友好提示"请上传 champions.json 和 traits.json"
- **锁定英雄超人口**：计算前校验，若 `Σ(locked slots) > population`，提示错误并阻止计算
- **拉克丝被排除但指定了双倍羁绊**：互斥校验，若拉克丝在排除列表中但配置了双倍羁绊，提示冲突并清空双倍配置
- **无可用英雄**：排除+锁定后无候选英雄时，返回空结果并提示
- **转职数 > 可用羁绊类型数**：多余转职无法分配，在结果中提示"X 个转职未使用"
- **计算超时**：Worker 中设置 5 秒硬超时，返回当前最优解并提示"已超时，结果为近似最优"
- **拉克丝不在阵容中但配置了双倍**：输入面板允许配置，但计算时若结果不含拉克丝则忽略该配置（UI 中显示灰色提示"当前阵容不含拉克丝，双倍配置未生效"）

---

## 9. 输出要求

请生成一个**完整的、可独立运行的 Vite + React 项目**，包含：
1. 完整的 `package.json`（含 scripts: dev/build/preview）
2. 完整的 `tsconfig.json` 和 `vite.config.ts`
3. `index.html` 引入 Tailwind CDN 或配置 PostCSS
4. `src/main.tsx` 入口
5. 所有上述文件结构中的组件和模块
6. 默认附带一组**示例数据**（包含拉克丝和奥恩，以及大元素使、灵魂莲华、地狱火等羁绊），确保项目 `npm run dev` 后立即可见界面
7. 在 `README.md` 中说明：
   - 如何运行项目
   - 如何替换为自己的 `champions.json` 和 `traits.json`
   - 两套求解器的算法简述
   - 拉克丝"大元素使"机制的处理逻辑
   - 转职分配策略说明

**重要**：代码必须类型安全（无 `any` 滥用），算法部分必须有详细中文注释说明剪枝逻辑、评分计算和拉克丝处理逻辑，UI 必须美观且响应式。

---

## 10. 用户将提供的数据示例

我会提供类似以下格式的两个 JSON 文件，你的代码必须能直接解析：

**champions.json:**
```json
[
  {
    "id": "lux",
    "name": "拉克丝",
    "cost": 5,
    "traits": ["灵魂莲华", "地狱火", "永恒之森", "古神", "仙灵", "远古", "日食", "月食", "魔女", "大元素使"],
    "slots": 1
  },
  {
    "id": "ornn",
    "name": "奥恩",
    "cost": 4,
    "traits": ["永恒之森", "护卫"],
    "slots": 1
  }
]
```

**traits.json:**
```json
[
  {
    "id": "spirit",
    "name": "灵魂莲华",
    "thresholds": [3, 5, 7, 9, 11],
    "hasEmblem": true,
    "emblemName": "灵魂莲华纹章",
    "type": "general"
  },
  {
    "id": "inferno",
    "name": "地狱火",
    "thresholds": [2, 3],
    "hasEmblem": true,
    "emblemName": "地狱火纹章",
    "type": "general"
  },
  {
    "id": "elementalist",
    "name": "大元素使",
    "thresholds": [1],
    "hasEmblem": false,
    "type": "exclusive",
    "effect": {
      "description": "拉克丝登场时，从以下羁绊中随机选择一个获得双倍计数",
      "targetTraits": ["灵魂莲华", "日食", "月食", "地狱火", "黑荆棘", "永恒之森", "魔女", "花仙子", "野兽之灵"]
    }
  }
]
```

请确保 `traits.json` 中的 `name` 与 `champions.json` 中 `traits` 数组的字符串**完全匹配**（包括中文），你的解析逻辑基于此做 Map 索引。`effect.targetTraits` 中的字符串也必须与 `traits.json` 中对应羁绊的 `name` 完全匹配。
