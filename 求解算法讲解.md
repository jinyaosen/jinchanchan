# 金铲铲 S18 拼羁绊求解器 —— 实现流程与逻辑讲解

本文讲解项目里「羁绊最多 / 质量最强」两套阵容求解算法的实现流程、核心数据结构、剪枝逻辑和正确性边界。

所有代码路径均相对于仓库根目录 `src/`。

---

## 1. 总体调用链

用户点击「开始计算」后，数据流如下：

```
主线程 UI
  └─ store/gameStore.ts          （状态管理，触发 startSolve）
       └─ solvers/workerClient.ts （创建单例 Web Worker，发请求）
            └─ solvers/worker.ts   （Worker 入口，接收消息）
                 └─ solvers/workerMain.ts （按模式分发）
                      ├─ solvers/backtrackA.ts  → maxTraits（羁绊最多）
                      └─ solvers/backtrackB.ts  → maxQuality（质量最强）
                           └─ solvers/common.ts  （共享预处理 + 评估）
                           └─ solvers/greedy.ts  （贪心初始解）
                           └─ solvers/runtime.ts （超时 / 取消 / 进度）
```

关键点：求解在 **Web Worker** 中执行，不阻塞 UI。主线程只负责展示进度和最终结果。

- `workerClient.ts` 创建单例 Worker：`new Worker(new URL('./worker.ts', import.meta.url), { type: 'module' })`
- 请求带 `requestId`，用于忽略过期请求的返回。
- Worker 通过 `progress` / `result` / `error` 三类消息回传。

---

## 2. 数据预处理 `prepareSolverData`

文件：`solvers/common.ts:46`

每次求解前，把「英雄 + 羁绊 + 配置」加工成求解器专用结构 `SolverData`。

主要步骤：

1. **区分羁绊类型**
   - `general`：通用羁绊，计入目标函数（即「激活了几个羁绊」）。
   - `exclusive`：专属羁绊，不计入目标函数，只参与展示。
   - 为每个 `general` 羁绊分配固定索引，后续计数用 `Uint8Array` 追踪。

2. **分离锁定英雄与候选英雄**
   - 锁定英雄：`config.lockedHeroIds` 中的英雄，强制上场，先放入初始状态。
   - 候选英雄：其余英雄。

3. **费用曲线过滤（仅 `maxQuality` 模式）**
   - `costPolicy.ts` 定义人口 → 费用范围，例如：
     - 人口 ≤ 4：只用 1~2 费，0 个 5 费
     - 人口 ≤ 7：1~3 费，0 个 5 费
     - 人口 8：1~5 费，最多 1 个 5 费
     - 人口 9：3~5 费，最多 4 个 5 费
     - 人口 ≥ 10：4~5 费，5 费不限
   - `maxTraits` 模式 **不启用** 费用过滤（`enforceCostPolicy = false`），允许低费卡凑羁绊。

4. **候选排序**
   - 按「每个占位提供的 general 羁绊数密度」降序：`generalIndices.length / slots`
   - 密度相同按费用降序，再按 id 升序。
   - 排序有助于回溯更早找到好解，也让对称剪枝可行。

5. **预计算英雄信息**
   - `heroGeneralIndices[i]`：英雄的 general 羁绊索引数组。
   - `heroMasks[i]`：用 `BigInt` 位掩码表示英雄的 general 羁绊集合。
   - `heroSlots[i]`：英雄占位。
   - `heroCosts[i]`：英雄费用。
   - `heroSignatures[i]`：英雄签名，用于对称剪枝。

6. **拉克丝与大元素使**
   - 找到拉克丝 `findLux`。
   - 判断拉克丝是否可用（在锁定或候选列表中）。
   - 得到可被双倍的 general 羁绊列表 `luxDoubleTargets`。

7. **纹章计数**
   - 把用户选择的纹章 `config.emblemChoices` 映射成 `emblemCounts`（每个 general 羁绊的纹章数量）。
   - 记录已选纹章总数 `selectedEmblemTotal`。

---

## 3. 位掩码 `bitmask.ts`

为每个 general 羁绊分配一个 `BigInt` 位：

- `maskOf(index)`：`1n << BigInt(index)`
- `encodeTraits(indices)`：把英雄的羁绊索引集合压成一个 `BigInt`
- `popcount(mask)`：数 `1` 的个数
- `andNot(mask, exclude)`：`mask & ~exclude`

用途：快速做并集/补集，判断「还剩哪些未激活羁绊」，用于剪枝上界计算。

---

## 4. 两个模式的目标函数

### 4.1 羁绊最多 `maxTraits`

目标：**最大化激活的 general 羁绊数量**，即 `activeTraits.length`。

平手时再比**总费用更高**（`common.ts:342` 的 `resultIsBetter`）：

```ts
if (b.activeTraits.length !== a.activeTraits.length) {
  return b.activeTraits.length > a.activeTraits.length;
}
return totalCost(b.heroes) > totalCost(a.heroes);
```

注意：`activeTraits` 只统计 `general` 羁绊（`common.ts:241` 只遍历 `generalTraits`），专属羁绊不计入数量。

### 4.2 质量最强 `maxQuality`

目标：**最大化质量分**，质量分由三部分相加（`scoring.ts:81`）：

```ts
total = costScore + traitScore + bonusScore
```

- `costScore`：英雄费用分，每个英雄 `cost * 10`。
- `traitScore`：羁绊层级分。每个 general 羁绊根据达到的 threshold 层级累计（15 / 35 / 60 / 90 / 120 逐级累加）。
- `bonusScore`：功能性加成（有 5 费 +20、4 费 ≥ 3 个 +15、激活 ≥ 8 羁绊 +25、人口利用率 ≥ 90% +10）。

---

## 5. 三段式求解流程

两个模式都走「贪心 → 回溯分支定界 → 局部搜索」三段。以 `maxTraits` 为例（`backtrackA.ts:20`）：

```
Phase 1: greedySolve        -> 快速得到可行解作为下界
Phase 2: backtrackA         -> 分支定界精确搜索
Phase 3: localSearchA       -> 单英雄替换微调
```

### 5.1 Phase 1：贪心初始解 `greedy.ts`

Best-first 贪心：

1. 从锁定英雄开始，构造初始计数和阵容。
2. 反复从未选英雄中挑「加入后目标函数提升最大」的一个，直到无法提升或人口已满。
3. 每次都用 `evaluateBest` 完整评估（含纹章、拉克丝双倍）。

作用：
- 立刻给 UI 一个可展示的近似解。
- 为后续回溯提供一个较好的下界 `best`，让剪枝更有效。

### 5.2 Phase 2：回溯分支定界

文件：`backtrackA.ts:47`（maxTraits）、`backtrackB.ts:75`（maxQuality）

这是核心。DFS 枚举所有候选英雄的组合（子集），并剪枝。

DFS 状态包含：

- `start`：当前考虑的第一个候选索引
- `counts`：当前 general 羁绊计数
- `used`：已用人口
- `selectedHeroes`：已选英雄
- `fiveCost`：已选 5 费数量（用于 `maxQuality` 费用限制）

每个节点执行：

1. `runtime.tick()` 检查是否超时 / 取消。
2. **上界剪枝**：估算「从当前节点继续，最多还能激活多少羁绊」，若不超过当前最优则直接返回（详见第 6 节）。
3. **评估当前节点**：`evaluateBest` 计算完整结果，若比当前最优更好则更新 `best` 并上报进度。
4. **分支**：遍历 `i = start .. n-1`，尝试加入候选英雄 `i`，递归 `dfs(i + 1, ...)`，回溯恢复状态。

分支时还有两类剪枝：

- **人口剪枝**：`used + slots[i] > population` 跳过。
- **对称剪枝**：`i > start && heroSignatures[i] === heroSignatures[i-1]` 跳过。相同签名（费用 + general 羁绊集合相同）的英雄对目标函数贡献相同，只保留第一个，避免重复枚举。
- **5 费限制**：`maxQuality` 模式下 `fiveCost >= maxFiveCost` 时跳过 5 费英雄。

### 5.3 Phase 3：局部搜索 `localSearchA/B`

文件：`backtrackA.ts:117`、`backtrackB.ts:178`

对回溯结果做「单英雄替换」微调：

- 取当前阵容中非锁定英雄，逐个尝试换成阵容外的候选英雄。
- 若替换后目标函数更好则接受，并重新开始（最多 3 轮，直到无改进）。

作用：弥补回溯因超时/剪枝可能没覆盖到的邻域解。

---

## 6. 剪枝上界详解

回溯能高效，核心是上界要「足够紧且不低估」（不低估才不会漏掉最优解）。

### 6.1 英雄潜在新增羁绊 `remainingCoverageUpperBound`

文件：`common.ts:292`

计算「从 `start` 开始，剩余英雄最多还能新激活多少个 general 羁绊」：

1. 对每个剩余英雄，计算其「尚未激活的羁绊」掩码：`newMask = heroMasks[i] & ~activeMask`。
2. 取所有 `newMask` 的并集，`distinct = popcount(unionMask)`：最多能覆盖的不同羁绊数。
3. 对每个英雄的 `newMask` 的 popcount 排序，取前 `remainingSlots` 个求和 `sumTopK`（剩余人口能上场的英雄数上界）。
4. 返回 `min(distinct, sumTopK)`。

这是一个**上界（可能高估）**，高估是安全的——只会少剪枝，不会错误剪掉最优解。

### 6.2 纹章潜在新增 `emblemActivationUpperBound`

文件：`common.ts:320`

计算「固定纹章在当前计数下，还能单独激活多少个尚未激活的羁绊」：

```ts
if (data.emblemCounts[i] > 0 && counts[i] + data.emblemCounts[i] >= thresholds[0]) reachable += 1;
```

### 6.3 拉克丝预留 `luxSlack`

`backtrackA.ts:72`：若拉克丝可用，上界额外 +1，因为双倍可能多激活一个羁绊。

### 6.4 总上界

`backtrackA.ts:73`：

```ts
upperBound = activeCount + heroPotential + emblemPotential + luxSlack;
if (upperBound <= best.activeTraits.length) return;
```

`maxQuality` 的上界思路类似，但把「可新增羁绊数」换成「剩余人口 × 单位人口最大质量 + 纹章/拉克丝/功能加成裕量」（`backtrackB.ts:95`）。

---

## 7. 拉克丝双倍与纹章的处理

文件：`common.ts:178`、`common.ts:188`、`common.ts:399`

### 7.1 纹章

用户选择的纹章数量直接叠加到对应羁绊计数上：

```ts
applyEmblems(counts, emblemCounts)  // next[i] += emblemCounts[i]
```

纹章在 `finalizeResult` 里统一叠加，作为「固定加成」，不参与每个英雄的分配决策。

### 7.2 拉克丝双倍

拉克丝登场时，可选择一个 target 羁绊获得 **+2** 计数（`applyLuxDouble`）。

`chooseBestLuxDouble` 的决策逻辑：

- 阵容里没有拉克丝 → `null`（不触发）。
- 用户配置为 `''` → `null`（明确不触发）。
- 用户指定了具体羁绊 → 直接使用。
- 用户配置为 `null`（自动）→ 遍历所有 `luxDoubleTargets`，选让目标函数最大的那个。

### 7.3 评估入口 `evaluateBest`

`common.ts:387`：先选最优拉克丝双倍，再 `evaluateWithDouble` 组装完整 `SolverResult`。

`finalizeResult` 里：
1. 叠加拉克丝双倍。
2. 叠加纹章。
3. 计算每个羁绊最终计数。
4. 统计激活羁绊 `activeTraits` 与未激活羁绊 `inactiveTraits`（按差几级激活排序）。
5. `maxQuality` 模式额外计算质量分和分项明细。

---

## 8. 运行时控制 `runtime.ts`

- `timeLimitMs`：超时上限。主线程设置见 `workerClient.ts` 的 `SOLVE_TIME_LIMIT_MS`。
- `tick()`：每 1500 个节点 `await setTimeout(0)` 让出事件循环，让 Worker 能处理取消消息、UI 保持响应。
- `shouldStop()`：超时或取消时返回 true，回溯立即停止。
- `timedOut()`：判断是否超时。

超时后：

```ts
best = { ...best, timedOut: true, approximate: true };
```

UI 会显示「计算完成（近似最优）」。这是**唯一**让结果不保证严格最优的来源。

---

## 9. 正确性与局限

### 正确性结论

- 回溯分支定界在**没有超时**的情况下，能找到目标函数下的严格最优解。
- 所有剪枝上界都是「高估」，不会错误剪掉最优分支。
- 对称剪枝只移除等价冗余分支，不影响正确性。

### 已知局限

1. **3 秒 / 5 秒超时**：人口高、候选多时，回溯可能没跑完，返回近似解。
2. **`activeTraits` 只数 `general` 羁绊**：专属羁绊不计入「羁绊最多」数量，这是设计决定（`types.ts:26`、`common.ts:241`）。
3. **纹章是固定加成**：不按英雄逐一分配，而是直接把用户选择的纹章数量加到羁绊计数上（`common.ts:178`）。这是简化建模。
4. **`maxQuality` 的上界较松**：`maxQualityPerSlot` 用单英雄最优密度做上界，实际可能高估较多，导致剪枝效率不如 `maxTraits`。

---

## 10. 关键文件速查

| 文件 | 职责 |
| --- | --- |
| `solvers/workerClient.ts` | 主线程 Worker 管理、超时常量 |
| `solvers/worker.ts` | Worker 消息入口 |
| `solvers/workerMain.ts` | 按模式分发求解 |
| `solvers/common.ts` | 预处理、计数、评估、剪枝上界、结果比较 |
| `solvers/greedy.ts` | 贪心初始解 |
| `solvers/backtrackA.ts` | 羁绊最多：回溯 + 局部搜索 |
| `solvers/backtrackB.ts` | 质量最强：回溯 + 局部搜索 |
| `solvers/runtime.ts` | 超时 / 取消 / 进度控制 |
| `utils/bitmask.ts` | BigInt 位掩码工具 |
| `utils/costPolicy.ts` | 人口 → 费用曲线 |
| `utils/luxEffect.ts` | 拉克丝双倍相关工具 |
| `utils/scoring.ts` | 质量分计算 |
